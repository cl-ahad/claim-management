Placeholder — see backend-ci.yml.txt in this folder for the ready-to-activate CI workflow.
This probe confirmed the connected token can write normal files but is rejected (404) on
.github/workflows/ paths, i.e. it lacks the GitHub `workflow` OAuth scope.
